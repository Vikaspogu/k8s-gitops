---
- include: locale.yml
  tags:
    - locale

- include: packages.yml
  tags:
    - packages

- include: network.yml
  tags:
    - network

- include: filesystem.yml
  tags:
    - filesystem

- include: user.yml
  tags:
    - user

- include: coral-host-setup.yml
  tags:
    - coral-host-setup
  when:
    - coral.enabled is defined
    - coral.enabled

- include: rsyslog.yml
  tags:
    - rsyslog
  when:
    - rsyslog.enabled is defined
    - rsyslog.enabled
