---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kimai-mysql-data
  namespace: default
  labels:
    app.kubernetes.io/instance: kimai-mysql
    app.kubernetes.io/name: kimai-mysql
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 2Gi
