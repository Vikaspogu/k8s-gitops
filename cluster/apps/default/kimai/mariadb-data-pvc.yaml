---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kimai-mariadb-data
  namespace: default
  labels:
    app.kubernetes.io/instance: kimai-mariadb
    app.kubernetes.io/name: kimai-mariadb
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
