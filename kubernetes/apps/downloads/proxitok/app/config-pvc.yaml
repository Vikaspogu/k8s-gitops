---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: proxitok-config
  namespace: downloads
  labels:
    app.kubernetes.io/instance: proxitok
    app.kubernetes.io/name: proxitok
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 1Gi
