---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # - ./config-pvc.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: llama-model-config
    files:
      - ./model-config.json
generatorOptions:
  disableNameSuffixHash: true
