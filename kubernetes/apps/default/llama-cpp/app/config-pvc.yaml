---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 75Gi
  storageClassName: ceph-block
