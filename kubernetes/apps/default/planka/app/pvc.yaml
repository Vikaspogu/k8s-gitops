---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: planka-data
  labels:
    app.kubernetes.io/instance: &name planka
    app.kubernetes.io/name: *name
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 2Gi
