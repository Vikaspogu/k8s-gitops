---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: papra-data
  labels:
    app.kubernetes.io/instance: &name papra
    app.kubernetes.io/name: *name
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 2Gi
