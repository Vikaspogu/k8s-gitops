---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-data
  labels:
    app.kubernetes.io/instance: &name kavita
    app.kubernetes.io/name: *name
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 2Gi
