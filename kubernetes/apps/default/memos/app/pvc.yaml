---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos-config
  namespace: default
  labels:
    app.kubernetes.io/name: &name memos
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 1Gi
