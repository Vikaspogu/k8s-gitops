---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "nas"
provisioner: nfs.csi.k8s.io
parameters:
  server: "${SECRET_NAS_ADDRESS}"
  share: "/volume1/media/k8s-data/"
reclaimPolicy: Delete
volumeBindingMode: Immediate
mountOptions: ["hard", "noatime"]
